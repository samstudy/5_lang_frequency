# 5_lang_frequency


Library:
1.import os for operating system dependent functionality
2.import re to provides regular expression matching operations
3.import collections to implements specialized container datatypes
4.from collections import Counter for a dict subclass for counting hashable objects

Functions:
1.def load_data(filepath)--This functin will upload a text in which should count a words
2.def get_most_frequent_words()---Will find the most frequently words also it will
provide the path where to need upload data ,user should define the path in load_data 
where exist a data.
3.if __name__ == '__main__'--will show the result
